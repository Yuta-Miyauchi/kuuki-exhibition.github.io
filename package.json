{
  "name": "read-the-room-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check": "npm run check:html && npm run check:css && npm run check:js",
    "check:html": "htmlhint \"*.html\"",
    "check:css": "stylelint \"css/**/*.css\"",
    "check:js": "eslint \"js/**/*.js\""
  },
  "devDependencies": {
    "eslint": "^10.9.1",
    "htmlhint": "^1.9.2",
    "stylelint": "^17.14.1"
  }
}
